ideaJunit {
    tasks = ['classes', 'pluginUnderTestMetadata']
}

ideaProjectComponents { components ->
    ["codeStyle", "gradle", "copyright"].each {
        components.file "gradle/idea/${it}.xml"
    }
}

idea {
    project {
        vcs = 'Git'
    }
}